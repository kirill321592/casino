export function deductBalance(balance: number, amount: number): number {
  return balance - amount
}

export function creditBalance(balance: number, amount: number): number {
  return balance + amount
}
